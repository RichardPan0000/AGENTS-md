# 全局工作规范

## 一、长周期 / 大改动任务识别规则

当任务满足以下任一条件时，将其视为“长周期任务”或“大改动任务”：

- 会修改多个文件
- 需要先分析、澄清、规划，再实施
- 预期不能在一次简单迭代中完成
- 会影响架构、接口、数据流、部署方式、核心逻辑
- 需要跨多个模块协同修改
- 需要持续记录进度、风险、决策和下一步动作
- 需要能够在中断后恢复上下文

对于这类任务，在正式实现前，必须先建立任务工作记录目录和文档。

---

## 二、任务工作记录目录规范

对于长周期 / 大改动任务，先创建任务目录：

`worklog/<任务标识-日期>/`

其中 `<任务标识-日期>` 应使用简短、清晰、可读的 kebab-case 命名方式，日期取当前日期，例如：

- `billing-refactor-20260509`
- `auth-timeout-fix-20260509`
- `pv-iv-diagnosis-upgrade-20260509`

在该目录下默认维护以下文件：

- `worklog/<任务标识-日期>/proposal.md`
- `worklog/<任务标识-日期>/spec.md`
- `worklog/<任务标识-日期>/design.md`
- `worklog/<任务标识-日期>/tasks.md`
- `worklog/<任务标识-日期>/status.md`

如果文件已存在，则不要覆盖，应该先读取并在原有基础上继续完善。

---

## 三、五个文件的职责

### 1. proposal.md

用于记录“为什么值得做这个任务”，至少包括：

- 背景
- 问题陈述
- 目标结果
- 业务 / 使用价值
- 为什么现在做

### 2. spec.md

用于记录“本次变更需要满足什么”，至少包括：

- 背景
- 目标
- 范围
- 非目标
- 约束条件
- 验收标准
- 与现状相比的关键变更点
- 相关文档 / 文件 / 上下游影响

### 3. design.md

用于记录“准备怎么做”，至少包括：

- 总体方案
- 模块边界 / 接口影响
- 数据流或关键处理流程
- 关键权衡
- 风险与回退思路
- 待确认技术问题

### 4. tasks.md

用于记录“接下来具体做什么”，至少包括：

- 阶段划分
- 每阶段目标
- 可执行任务清单
- 里程碑
- 每项任务对应的验证方式
- 完成状态勾选

### 5. status.md

用于记录“当前做到哪里了”，至少包括：

- 当前状态
- 已完成内容
- 当前进行中内容
- 当前阻塞问题
- 最新决策
- 已执行验证及结果
- 下一步动作
- 最终结果与遗留风险（任务结束时补充）

---

## 四、初始化要求

新建上述文件时，不要创建空文件，至少写入默认章节骨架。

建议最少包含以下标题：

- `proposal.md`: 背景、问题陈述、目标结果、业务 / 使用价值、为什么现在做、不做的代价
- `spec.md`: 背景、目标、范围、非目标、约束条件、验收标准、与现状相比的关键变更点、相关文件 / 文档 / 上下游影响
- `design.md`: 总体方案、模块边界 / 接口影响、数据流 / 关键流程、关键技术决策、方案权衡、风险与回退思路、待确认问题
- `tasks.md`: 阶段划分、任务清单、里程碑、每项任务的验证方式
- `status.md`: 当前状态、已完成内容、当前进行中、当前阻塞问题、最新决策、已执行验证及结果、下一步动作、最终结果与遗留风险

---

## 五、兼容旧任务

如果已有旧任务目录使用的是：

- `spec.md`
- `plan.md`
- `status.md`

则不要粗暴重建。

继续推进旧任务前，应遵循以下规则：

- 保留已有 `plan.md`
- 将其中“方案 / 权衡 / 接口影响”内容迁移或映射到 `design.md`
- 将其中“执行步骤 / 里程碑 / 勾选事项”内容迁移或映射到 `tasks.md`
- 后续新增内容优先写入 `proposal.md`、`design.md`、`tasks.md`、`status.md`

`plan.md` 作为兼容旧流程的遗留文件存在，不再作为新长周期任务的默认核心文件。

---

## 六、长周期任务执行要求

### 开始实施前

在进行大规模改动前，必须先：

1. 识别任务是否属于长周期 / 大改动任务
2. 创建或复用 `worklog/<任务标识-日期>/`
3. 创建并初始化缺失的 `proposal.md`、`spec.md`、`design.md`、`tasks.md`、`status.md`
4. 先阅读这些文件，再进入正式实现阶段

### 实施过程中

- 每完成一个重要阶段后，更新 `status.md`
- 每完成一项任务或推进一个子任务后，更新 `tasks.md`
- 如果执行路径或技术方案发生变化，更新 `design.md`
- 如果需求范围或验收标准发生变化，先明确记录，再修改 `spec.md`
- 如果任务目标或立项理由发生变化，更新 `proposal.md`
- 不要在没有记录的情况下直接做大范围实现

### 收尾阶段

在宣布任务完成前，应尽量执行与任务相关的验证，例如：

- 测试
- lint
- typecheck
- build
- 最小复现验证
- 关键路径回归检查

并在 `status.md` 中补充：

- 实际完成情况
- 已执行验证与结果
- 剩余问题
- 风险说明
- 建议的下一步
- 当前任务是否已完成 / archived

如果仓库中存在长期基线文档、长期 spec 或决策文档，应尽量把本次最终结论同步回长期文档，而不是只停留在 `worklog` 中。

---

## 七、小任务处理规则

对于以下任务，一般不需要创建 `worklog`：

- 单文件小修复
- 明确且很短的文本修改
- 极小范围的配置调整
- 一次即可完成的小改动

但如果用户明确要求记录过程，或者任务虽然小但需要保留决策过程，仍然可以创建 `worklog`。

---

## 八、输出风格要求

在处理长周期任务时：

- 先给出结构化分析，再开始实施
- 尽量说明为什么这样规划
- 改动前说明范围
- 改动后说明验证结果
- 对不确定事项明确标记，不要假设已确认
- 若是续做已有任务，明确说明本次是在延续哪个 `worklog`

---

## 九、完成定义

对于长周期任务，只有在以下条件基本满足时，才可视为完成：

- `proposal.md`、`spec.md`、`design.md`、`tasks.md`、`status.md` 已建立并更新
- 主要代码 / 文档 / 配置改动已完成
- 关键验证已执行或明确说明未执行原因
- 当前结果、风险和后续建议已写入 `status.md`
- 若存在长期基线文档，已同步关键决策，或已明确说明暂未同步原因

---

## 十、优先级原则

若当前目录或仓库内存在更具体的 `AGENTS.md`、项目规范或任务说明，则优先遵循更具体的规则；
本文件作为全局默认规范使用。
