# 全局工作规范

## 一、长周期 / 大改动任务识别规则

当任务满足以下任一条件时，将其视为“长周期任务”或“大改动任务”：

- 会修改多个文件
- 需要先分析、规划，再实施
- 预期不能在一次简单迭代中完成
- 会影响架构、接口、数据流、部署方式、核心逻辑
- 需要跨多个模块协同修改
- 需要持续记录进度、风险、决策和下一步动作

对于这类任务，在正式实现前，必须先建立任务工作记录目录和文档。

---

## 二、任务工作记录目录规范

对于长周期 / 大改动任务，先创建任务目录：

`worklog/<任务标识>/`

其中 `<任务标识>` 应使用简短、清晰、可读的 kebab-case 命名方式，例如：

- `billing-refactor`
- `auth-timeout-fix`
- `pv-iv-diagnosis-upgrade`

在该目录下创建以下文件：

- `worklog/<任务标识>/spec.md`
- `worklog/<任务标识>/plan.md`
- `worklog/<任务标识>/status.md`

如果文件已存在，则不要覆盖，应该先读取并在原有基础上继续完善。

---

## 三、三个文件的职责

### 1. spec.md

用于记录任务目标与约束，至少包括：

- 背景
- 目标
- 范围
- 非目标
- 约束条件
- 验收标准
- 相关文档 / 文件 / 上下游影响

### 2. plan.md

用于记录执行计划，至少包括：

- 阶段划分
- 每阶段目标
- 里程碑
- 验证方式
- 风险与依赖
- 待确认事项

### 3. status.md

用于记录当前进展，至少包括：

- 已完成内容
- 当前进行中内容
- 当前阻塞问题
- 最新决策
- 下一步动作
- 最终结果与遗留风险（任务结束时补充）

---

## 四、长周期任务执行要求

### 开始实施前

在进行大规模改动前，必须先：

1. 识别任务是否属于长周期 / 大改动任务
2. 创建 `worklog/<任务标识>/`
3. 创建并初始化 `spec.md`、`plan.md`、`status.md`
4. 先阅读这三个文件，再进入正式实现阶段

### 实施过程中

- 每完成一个重要阶段后，更新 `status.md`
- 如果执行路径发生变化，更新 `plan.md`
- 如果需求本身发生变化，先明确记录，再修改 `spec.md`
- 不要在没有记录的情况下直接做大范围实现

### 收尾阶段

在宣布任务完成前，应尽量执行与任务相关的验证，例如：

- 测试
- lint
- typecheck
- build
- 最小复现验证
- 关键路径回归检查

并在 `status.md` 中补充：

- 实际完成情况
- 剩余问题
- 风险说明
- 建议的下一步

---

## 五、小任务处理规则

对于以下任务，一般不需要创建 `worklog`：

- 单文件小修复
- 明确且很短的文本修改
- 极小范围的配置调整
- 一次即可完成的小改动

但如果用户明确要求记录过程，或者任务虽然小但需要保留决策过程，仍然可以创建 `worklog`。

---

## 六、输出风格要求

在处理长周期任务时：

- 先给出结构化分析，再开始实施
- 尽量说明为什么这样规划
- 改动前说明范围
- 改动后说明验证结果
- 对不确定事项明确标记，不要假设已确认

---

## 七、完成定义

对于长周期任务，只有在以下条件基本满足时，才可视为完成：

- `spec.md`、`plan.md`、`status.md` 已建立并更新
- 主要代码 / 文档 / 配置改动已完成
- 关键验证已执行或明确说明未执行原因
- 当前结果、风险和后续建议已写入 `status.md`

---

## 八、优先级原则

若当前目录或仓库内存在更具体的 `AGENTS.md`、项目规范或任务说明，则优先遵循更具体的规则；
本文件作为全局默认规范使用。
